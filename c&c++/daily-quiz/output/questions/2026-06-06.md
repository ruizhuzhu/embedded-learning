# 嵌入式 C/C++ 每日练习 — 2026-06-06

**今日焦点：** 嵌入式系统核心
**次要分类：** C 语言核心, 数据结构与算法
**难度分布：** 中等 ★★☆, 中等 ★★☆, 简单 ★☆☆, 中等 ★★☆
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 选择题 — ★★☆
**分类：** 嵌入式系统核心 / rtos-task-scheduling
**标签：** `rtos` `scheduler` `priority` `preemptive`

在 FreeRTOS 的抢占式调度（Preemptive Scheduling）中，什么事件会触发任务切换？

A. 仅定时器 SysTick 中断
B. 高优先级任务就绪（如信号量释放、消息到达）、当前任务阻塞（如 vTaskDelay）、或 SysTick 时间片到期
C. 仅当当前任务主动调用 taskYIELD() 时
D. 仅当发生硬件中断时

---

## 第 2 题 — 选择题 — ★★☆
**分类：** 嵌入式系统核心 / bootloader
**标签：** `bootloader` `firmware-update` `flash`

嵌入式系统中，Bootloader 的核心职责是什么？"双区备份"（dual-bank）固件升级策略的优势是什么？

A. Bootloader 仅负责初始化时钟，双区备份节省 Flash 空间
B. Bootloader 负责引导应用程序和固件升级，双区备份确保升级失败时仍可回滚到旧固件
C. Bootloader 仅在开发阶段使用，量产时移除
D. 双区备份意味着同时运行两个固件以提高性能

---

## 第 3 题 — 选择题 — ★☆☆
**分类：** C 语言核心 / memory-layout
**标签：** `memory` `stack` `heap` `bss`

嵌入式程序的典型内存布局中，未初始化的全局变量存储在哪个段？

A. .text 段
B. .data 段
C. .bss 段
D. .stack 段

---

## 第 4 题 — 编程题 — ★★☆
**分类：** 数据结构与算法 / state-machine
**标签：** `state-machine` `fsm` `design-pattern`

嵌入式系统中，有限状态机（FSM）是处理复杂逻辑的常用方法。请用 C 语言实现一个简单的事件驱动型状态机框架，支持：
1. 状态转移表（State Transition Table）
2. 进入/退出动作
3. 使用函数指针实现状态处理

以按键处理为例：短按切换 LED、长按进入设置模式。

```c
// 状态、事件枚举
typedef enum { STATE_IDLE, STATE_SHORT_PRESS, STATE_LONG_PRESS } State;
typedef enum { EVENT_PRESS, EVENT_RELEASE, EVENT_TIMEOUT } Event;

// 请实现状态机框架和按键状态转移表
```

---

*生成于 2026-06-06 | 题库 ID: embed_003, embed_005, c_011, ds_003*
