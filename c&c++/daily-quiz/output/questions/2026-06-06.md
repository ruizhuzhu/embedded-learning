# 嵌入式 C/C++ 每日练习 — 2026-06-06

**今日焦点：** 实战编程题
**次要分类：** 外设与通信协议, 调试与工具
**难度分布：** 中等 ★★☆, 简单 ★☆☆, 简单 ★☆☆, 简单 ★☆☆
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 编程题 — ★★☆
**分类：** 实战编程题 / memory-pool
**标签：** `memory-pool` `static-allocation` `no-malloc`

实现一个固定大小的内存池分配器（Fixed-Size Block Allocator），要求：
1. 预分配 N 个相同大小的内存块
2. 分配/释放操作 O(1) 时间复杂度
3. 使用空闲链表管理（free list）
4. 不使用 malloc，所有内存在编译时确定

```c
#define BLOCK_SIZE  64
#define BLOCK_COUNT 16

typedef struct {
    // 请定义内存池结构
} MemPool;

void  mempool_init(MemPool *pool);
void *mempool_alloc(MemPool *pool);
void  mempool_free(MemPool *pool, void *ptr);
```

---

## 第 2 题 — 编程题 — ★☆☆
**分类：** 实战编程题 / button-debounce
**标签：** `debounce` `button` `timer` `gpio`

机械按键在按下和释放时会产生抖动（通常 5-20ms）。请实现一个消抖函数，要求：
1. 使用软件延时去抖（非阻塞方式，使用定时器计数）
2. 返回稳定的按键状态
3. 支持检测按下事件（下降沿）和释放事件（上升沿）
4. 不要使用阻塞延时（delay_ms）

```c
#define DEBOUNCE_MS 20

typedef struct {
    // 请定义去抖所需的状态变量
} ButtonDebounce;

// 每 1ms 调用一次（放在定时器 ISR 或主循环轮询）
bool button_debounce(ButtonDebounce *btn, bool current_raw_state) {
    // 请实现返回去抖后的稳定状态
}

// 检测按下事件
bool button_is_pressed(ButtonDebounce *btn);
```

---

## 第 3 题 — 选择题 — ★☆☆
**分类：** 外设与通信协议 / pwm
**标签：** `pwm` `timer` `duty-cycle`

PWM 输出中，占空比（Duty Cycle）和频率分别由定时器的哪些参数决定？在边沿对齐（Edge-Aligned）模式下，PWM 频率如何计算？

A. 占空比由 ARR（自动重载值）决定，频率由 CCR（捕获比较值）决定
B. 频率 = 定时器时钟 / (ARR + 1)，占空比 = CCR / (ARR + 1)
C. 占空比和频率都由预分频器 PSC 单独决定
D. 占空比 = (ARR - CCR) / ARR，频率无法计算

---

## 第 4 题 — 选择题 — ★☆☆
**分类：** 调试与工具 / assert
**标签：** `assert` `debugging` `defensive-programming`

在嵌入式 C 开发中，`assert()` 宏的主要用途是什么？为什么发布版本通常会禁用 assert？

A. 处理运行时错误（如文件不存在）
B. 在开发阶段捕获逻辑错误和违反前置条件的 bug，发布版通过 NDEBUG 禁用以减少代码体积和开销
C. 替代所有错误处理机制
D. 实现单元测试

---

*生成于 2026-06-06 | 题库 ID: code_004, code_001, periph_007, dbg_002*
