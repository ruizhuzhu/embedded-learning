# 嵌入式 C/C++ 每日练习 — 2026-06-10

**今日焦点：** C 语言核心
**次要分类：** 调试与工具, 外设与通信协议
**难度分布：** 中等 ★★☆, 中等 ★★☆, 中等 ★★☆, 中等 ★★☆
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 选择题 — ★★☆
**分类：** C 语言核心 / const
**标签：** `const` `pointer` `type-qualifier`

以下声明中，哪个表示"指针本身不可修改，但指向的数据可修改"？

A. `const int *p;`
B. `int const *p;`
C. `int *const p;`
D. `const int *const p;`

---

## 第 2 题 — 编程题 — ★★☆
**分类：** C 语言核心 / bit-manipulation
**标签：** `bit-manipulation` `macro` `register`

请编写一个宏 `SET_BIT(reg, bit)` 将寄存器 `reg` 的第 `bit` 位置 1，以及一个宏 `CLEAR_BIT(reg, bit)` 将第 `bit` 位清零，再写一个 `TOGGLE_BIT(reg, bit)` 翻转第 `bit` 位。要求宏能正确处理各种参数类型。

```c
// 请在下方编写三个宏
#define SET_BIT(reg, bit)     
#define CLEAR_BIT(reg, bit)   
#define TOGGLE_BIT(reg, bit)
```

---

## 第 3 题 — 选择题 — ★★☆
**分类：** C 语言核心 / struct-union
**标签：** `struct` `memory-alignment` `padding`

在 32 位 ARM 处理器上，以下结构体的 sizeof 是多少？
```c
struct Example {
    char a;
    int  b;
    char c;
    short d;
};
```

A. 8 字节
B. 10 字节
C. 12 字节
D. 16 字节

---

## 第 4 题 — 选择题 — ★★☆
**分类：** 外设与通信协议 / can
**标签：** `can` `arbitration` `identifier` `automotive`

CAN 总线使用非破坏性逐位仲裁（Non-Destructive Bitwise Arbitration）。当两个节点同时发送不同 ID 的帧时，仲裁机制如何决定谁赢得总线？

A. 随机退避，类似以太网 CSMA/CD
B. ID 越小的帧优先级越高——显性位（0）覆盖隐性位（1），发完仲裁域后仅胜者继续发送
C. 先到先得，谁先发送谁赢
D. 主节点决定优先级

---

*生成于 2026-06-10 | 题库 ID: c_003, c_005, c_006, periph_005*
