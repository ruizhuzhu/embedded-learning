# 嵌入式 C/C++ 每日练习 — 2026-06-08

**今日焦点：** 数据结构与算法
**次要分类：** 嵌入式系统核心, C 语言核心
**难度分布：** 中等 ★★☆, 中等 ★★☆, 困难 ★★★, 简单 ★☆☆
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 编程题 — ★★☆
**分类：** 数据结构与算法 / ring-buffer
**标签：** `ring-buffer` `circular-buffer` `lock-free`

实现一个无锁（Lock-Free）环形缓冲区，要求：
1. 单生产者单消费者模式（SPSC）
2. 使用 head 和 tail 指针，不需要 count 变量
3. 支持判断满和空
4. 如果不使用 count，如何区分"满"和"空"状态？请写出 push 和 pop 函数。

```c
typedef struct {
    uint8_t *buffer;
    uint32_t size;
    volatile uint32_t head;  // 生产者写入位置
    volatile uint32_t tail;  // 消费者读取位置
} RingBuf;

bool rb_push(RingBuf *rb, uint8_t data) {
    // 请实现
}

bool rb_pop(RingBuf *rb, uint8_t *data) {
    // 请实现
}
```

---

## 第 2 题 — 选择题 — ★★☆
**分类：** 数据结构与算法 / crc
**标签：** `crc` `error-detection` `communication`

嵌入式通信协议中，CRC（循环冗余校验）广泛用于错误检测。CRC-8、CRC-16、CRC-32 的主要区别是什么？哪种更适用于 Modbus 协议？

A. 区别仅在于校验值的位宽（8/16/32 位），适用场景取决于数据包大小和所需检错能力；Modbus 使用 CRC-16
B. 区别仅在于计算速度，选哪个都一样
C. CRC-32 最快，因为 32 位 MCU 一次处理 4 字节
D. 区别在于使用不同的位运算指令

---

## 第 3 题 — 简答题 — ★★★
**分类：** 嵌入式系统核心 / fpu-context-switch
**标签：** `fpu` `context-switch` `cortex-m4` `lazy-stacking`

ARM Cortex-M4F/M7 支持硬件浮点（FPU）。在 RTOS 上下文切换时，FPU 寄存器（S0-S31）如何处理？什么是"惰性压栈"（Lazy Stacking），它如何优化性能？

---

## 第 4 题 — 选择题 — ★☆☆
**分类：** C 语言核心 / volatile
**标签：** `volatile` `compiler-optimization` `memory-mapped-io`

在嵌入式 C 编程中，`volatile` 关键字的主要作用是什么？

A. 将变量存储在 ROM 而非 RAM 中
B. 告诉编译器每次访问该变量时都从内存读取，禁止优化掉看似多余的读写
C. 使变量成为只读，防止意外修改
D. 使变量可以在多个源文件中共享

---
B,volatile 禁止编译器进行优化
*生成于 2026-06-08 | 题库 ID: ds_001, ds_004, embed_011, c_001*
