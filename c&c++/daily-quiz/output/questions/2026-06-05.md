# 嵌入式 C/C++ 每日练习 — 2026-06-05

**今日焦点：** C 语言核心
**次要分类：** 嵌入式系统核心, 外设与通信协议
**难度分布：** 简单 ★☆☆, 简单 ★☆☆, 简单 ★☆☆, 困难 ★★★
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 选择题 — ★☆☆
**分类：** C 语言核心 / endianness
**标签：** `endianness` `byte-order` `network-protocol`

在一个大端（Big-Endian）系统中，32 位整数 `0x12345678` 在内存中的字节排列顺序（从低地址到高地址）是什么？

A. 12 34 56 78
B. 78 56 34 12
C. 34 12 78 56
D. 56 78 12 34

---

## 第 2 题 — 选择题 — ★☆☆
**分类：** C 语言核心 / union
**标签：** `union` `type-punning` `memory`

在嵌入式系统中，union 常用于类型双关（type punning）。以下代码的输出是什么？
```c
union {
    uint32_t word;
    uint8_t  bytes[4];
} data;
data.word = 0x12345678;
printf("%02X\n", data.bytes[0]);
```
在小端系统上运行。

A. 12
B. 78
C. 34
D. 56

---

## 第 3 题 — 选择题 — ★☆☆
**分类：** 嵌入式系统核心 / interrupts
**标签：** `interrupt` `isr` `latency`

以下关于中断服务程序（ISR）的说法，哪项是正确的？

A. ISR 中可以使用 printf() 来打印调试信息
B. ISR 应该尽可能短小精悍，避免长时间占用 CPU
C. ISR 中可以调用任何函数，只要加了 volatile 修饰
D. ISR 执行期间，当前任务会自动释放其持有的所有互斥锁

---

## 第 4 题 — 编程题 — ★★★
**分类：** 外设与通信协议 / timers
**标签：** `timer` `input-capture` `frequency-measurement`

使用 STM32 定时器的输入捕获（Input Capture）功能测量外部信号的频率。请用伪代码描述实现步骤和关键配置：
1. 配置定时器为输入捕获模式
2. 在捕获中断中计算频率
3. 处理定时器溢出（32 位定时器也可能溢出）

```c
// 假设 TIM2, CH1 用于输入捕获
// 请描述配置和 ISR 实现

void TIM2_Config(void) {
    // 配置步骤
}

void TIM2_IRQHandler(void) {
    // 捕获中断处理
}
```

---

*生成于 2026-06-05 | 题库 ID: c_009, c_018, embed_001, periph_008*
