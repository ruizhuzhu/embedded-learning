# 嵌入式 C/C++ 每日练习 — 2026-06-04

**今日焦点：** 嵌入式系统核心
**次要分类：** 实战编程题, 外设与通信协议
**难度分布：** 中等 ★★☆, 简单 ★☆☆, 简单 ★☆☆
**预计用时：** 24 分钟
**题目数量：** 3 题

---

## 第 1 题 — 简答题 — ★★☆
**分类：** 嵌入式系统核心 / startup-code
**标签：** `startup` `vector-table` `reset-handler`

请描述 ARM Cortex-M 处理器从上电到进入 main() 函数的完整启动流程，包括：复位向量、堆栈初始化、数据段初始化。为什么 .data 段需要从 Flash 复制到 RAM？

---

## 第 2 题 — 选择题 — ★☆☆
**分类：** 嵌入式系统核心 / power-management
**标签：** `low-power` `sleep-mode` `wake-up`

在低功耗嵌入式系统中，MCU 从 Sleep 模式被中断唤醒后，程序的执行流程是什么？

A. 从 main() 函数开头重新执行
B. 执行 ISR，然后返回到 WFI/WFE 指令之后的指令继续执行
C. 执行 ISR，然后触发系统复位
D. 跳过 ISR 直接继续执行

---

## 第 3 题 — 编程题 — ★☆☆
**分类：** 实战编程题 / endian-swap
**标签：** `endianness` `swap` `network-byte-order`

实现大小端转换函数：
1. `uint16_t swap16(uint16_t val)` — 16 位字节交换
2. `uint32_t swap32(uint32_t val)` — 32 位字节交换
3. 使用编译时检测（利用预定义宏）自动判断当前系统大小端
4. 写出 `htonl()` 和 `ntohl()` 的嵌入式版本（不使用系统库）

```c
// 请实现
uint16_t swap16(uint16_t val) { }
uint32_t swap32(uint32_t val) { }
```

---

*生成于 2026-06-04 | 题库 ID: embed_008, embed_009, code_003*
