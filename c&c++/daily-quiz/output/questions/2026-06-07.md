# 嵌入式 C/C++ 每日练习 — 2026-06-07

**今日焦点：** 外设与通信协议
**次要分类：** C 语言核心, 实战编程题
**难度分布：** 简单 ★☆☆, 简单 ★☆☆, 简单 ★☆☆, 困难 ★★★
**预计用时：** 32 分钟
**题目数量：** 4 题

---

## 第 1 题 — 选择题 — ★☆☆
**分类：** 外设与通信协议 / gpio
**标签：** `gpio` `pull-up` `pull-down` `open-drain`

STM32 GPIO 的"开漏输出"（Open-Drain）模式的主要应用场景是什么？

A. 驱动 LED，提供最大亮度
B. 实现 I2C 总线、电平转换、多设备共享单线（线与逻辑）
C. 高速数据传输（比推挽输出更快）
D. 仅用于输入模式

---

## 第 2 题 — 简答题 — ★☆☆
**分类：** 外设与通信协议 / uart
**标签：** `uart` `baud-rate` `framing-error`

UART 通信中，常见的"帧错误"（Framing Error）和"溢出错误"（Overrun Error）分别是什么原因导致的？波特率误差如何影响通信可靠性？

---

## 第 3 题 — 简答题 — ★☆☆
**分类：** C 语言核心 / array-size
**标签：** `array` `macro` `sizeof`

请说明为什么在 C 语言中推荐使用 `sizeof(array)/sizeof(array[0])` 来获取数组元素个数，而不是直接使用硬编码的数字。并写出一个安全的宏 `ARRAY_SIZE(x)`。

---

## 第 4 题 — 编程题 — ★★★
**分类：** 实战编程题 / binary-protocol
**标签：** `protocol` `framing` `serial` `parser`

设计并实现一个简单的二进制协议帧解析器。协议格式如下：
- 起始标志：0xAA 0x55（2 字节）
- 数据长度：1 字节（N，表示 Payload 长度，范围 0-255）
- Payload：N 字节
- 校验和：1 字节（从长度字节开始到 Payload 末尾的所有字节的 XOR）

要求：
1. 实现状态机逐字节解析
2. 处理字节填充（数据中出现 0xAA 时转义为 0xAA 0x00）
3. 返回完整的 Payload 和解析结果
4. 能处理不完整数据流（粘包/半包）

```c
typedef enum {
    STATE_IDLE,
    STATE_HEADER,
    STATE_LENGTH,
    STATE_PAYLOAD,
    STATE_CHECKSUM
} ParserState;

// 请实现完整的解析器
```

---

*生成于 2026-06-07 | 题库 ID: periph_001, periph_004, c_020, code_002*
